## Rules

### Python

- NEVER invoke `python`, `python3`, `pip`, or `pip3` directly
- ONLY use `uv run` and `uv add` for these needs

### Tracking progress

- Update the `CHANGELOG.md`
- Mark the work done in `PLAN.md`